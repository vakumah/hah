name: Windows RDP Ultimate God Mode (Zero Lag)

on:
  workflow_dispatch:

jobs:
  setup-rdp-tunnel:
    runs-on: windows-2025

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Extreme Optimization (Defender, Network, & CPU)
      run: |
        # 1. MATIKAN WINDOWS DEFENDER & FIREWALL (Biar CPU 100% buat RDP)
        Set-MpPreference -DisableRealtimeMonitoring $true
        Set-MpPreference -DisableBehaviorMonitoring $true
        Set-MpPreference -DisableIOAVProtection $true
        Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled False

        # 2. PAKSA COLOR DEPTH 16-BIT & LOW ENCRYPTION
        $rdpKey = "HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services"
        if (!(Test-Path $rdpKey)) { New-Item -Path $rdpKey -Force }
        Set-ItemProperty -Path $rdpKey -Name "ColorDepth" -Value 2
        Set-ItemProperty -Path $rdpKey -Name "MinEncryptionLevel" -Value 1

        # 3. TCP LOW LATENCY (Hapus delay mouse & keyboard)
        $tcpKey = "HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters\Interfaces"
        Get-ChildItem $tcpKey | ForEach-Object {
            New-ItemProperty -Path $_.PSPath -Name "TcpAckFrequency" -Value 1 -PropertyType DWord -Force -ErrorAction SilentlyContinue
            New-ItemProperty -Path $_.PSPath -Name "TCPNoDelay" -Value 1 -PropertyType DWord -Force -ErrorAction SilentlyContinue
        }

        # 4. MATIKAN SEMUA EFEK VISUAL (Mode Windows Klasik)
        Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name 'UserPreferencesMask' -Value ([byte[]](0x90,0x12,0x01,0x80,0x10,0x00,0x00,0x00))
        Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name 'FontSmoothing' -Value 0
        Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name 'Wallpaper' -Value ""
        Set-ItemProperty -Path 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects' -Name 'VisualFXSetting' -Value 2

        # 5. PRIORITAS CPU TINGGI UNTUK RDP & MATIKAN SERVICE BERAT
        $perfKey = "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Image File Execution Options\termsrv.exe\PerfOptions"
        if (!(Test-Path $perfKey)) { New-Item -Path $perfKey -Force }
        Set-ItemProperty -Path $perfKey -Name "CpuPriorityClass" -Value 3
        
        $services = "WSearch","SysMain","DiagTrack","dmwappushservice","wuauserv","BITS","Spooler","Themes"
        foreach ($svc in $services) {
          Set-Service -Name $svc -StartupType Disabled -ErrorAction SilentlyContinue
          Stop-Service -Name $svc -Force -ErrorAction SilentlyContinue
        }

        # 6. TWEAK MTU & POWER PLAN
        netsh interface ipv4 set subinterface "Ethernet" mtu=1400 store=persistent
        powercfg /setactive SCHEME_MIN

    - name: Setup User & RDP
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "p@ssw0rd!" -Force)

    - name: Start Playit Tunnel
      env:
        PLAYIT_AUTH_KEY: ${{ secrets.PL }}
      run: |
        Invoke-WebRequest -Uri "https://github.com/playit-cloud/playit-agent/releases/download/v0.15.26/playit-windows-x86_64-signed.exe" -OutFile "$env:USERPROFILE\playit.exe"
        Start-Process -FilePath "$env:USERPROFILE\playit.exe" -ArgumentList "--secret $env:PLAYIT_AUTH_KEY" -WindowStyle Hidden
        Start-Sleep -Seconds 10

    - name: Keep Alive
      run: |
        Write-Output "RDP God Mode is Active! User: runneradmin | Pass: p@ssw0rd!"
        while ($true) {
          Get-Process playit -ErrorAction SilentlyContinue
          Start-Sleep -Seconds 300
        }
        # Network tweaks
        Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Name 'MaxUserPort' -Value 65534
        Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Name 'TcpTimedWaitDelay' -Value 30
        # Power mode High Performance
        powercfg /setactive SCHEME_MIN
        # Check RDP service
        Start-Service TermService
        Set-Service TermService -StartupType Automatic

    - name: Start Playit Tunnel (Background)
      env:
        PLAYIT_AUTH_KEY: ${{ secrets.PL }}
      run: |
        Start-Process -FilePath "$env:USERPROFILE\playit.exe" -ArgumentList "--secret $env:PLAYIT_AUTH_KEY" -WindowStyle Hidden
        Start-Sleep -Seconds 10
        Get-Process playit

    - name: Keep GitHub Runner Alive
      run: |
        Write-Output "RDP + Playit is running... Keep Alive enabled."
        while ($true) {
          Start-Sleep -Seconds 300
        }
